<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="barecode_tare.list">
      <field name="name">barecode_tare list</field>
      <field name="model">barecode_tare.barecode_tare</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="barecode_tare.action_window">
      <field name="name">barecode_tare window</field>
      <field name="res_model">barecode_tare.barecode_tare</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="barecode_tare.action_server">
      <field name="name">barecode_tare server</field>
      <field name="model_id" ref="model_barecode_tare_barecode_tare"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="barecode_tare" id="barecode_tare.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="barecode_tare.menu_1" parent="barecode_tare.menu_root"/>
    <menuitem name="Menu 2" id="barecode_tare.menu_2" parent="barecode_tare.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="barecode_tare.menu_1_list" parent="barecode_tare.menu_1"
              action="barecode_tare.action_window"/>
    <menuitem name="Server to list" id="barecode_tare" parent="barecode_tare.menu_2"
              action="barecode_tare.action_server"/>
    -->
  </data>
</odoo>